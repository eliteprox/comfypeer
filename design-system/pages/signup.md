# Page override: `/signup`

## Job

Create human session → provision PymtHouse end-user → land in studio.

## Verification (Decision #6)

Verification method is a **visible** design input (card-on-file / GitHub age / phone). Until decided, reserve a step slot labelled "Verify you're human" — do not imply agents can use this path for free credit.

## Form UX

- Visible labels (not placeholder-only)
- Errors via `role="alert"`
- Focus ring `--color-ring`
- Post-signup: first-run points at **start a stream**, not empty dashboard KPIs
