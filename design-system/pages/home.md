# Page override: `/` (Home)

> Overrides `MASTER.md` for the marketing homepage only.

## Job

Convert on **latency + live demo**, not price essays. Hero is the product.

## Hero composition (first viewport)

Exactly:
1. Logomark + wordmark (brand-level)
2. One H1 (revised one-liner short form)
3. One supporting sentence (bill-by-second + live prompt update)
4. CTA group: `[ Start free ]` · `[ Open studio ]` (or Import workflow)
5. **Dominant live demo:** webcam in | prompt | live output | cost ticker

Do **not** put plan stats, channel logos, or feature grids in the first viewport.

## Demo widget requirements

- Webcam (or sample stream) → processed output side-by-side
- Prompt input; `update_params` mid-stream is the wow moment
- Mono cost ticker (`$0.031` style) + elapsed seconds + FPS
- Warmup overlay is honest (measure TTFF; show it)
- No signup; per-IP daily budget from owner wallet
- Pause + reduced-motion freeze

## Below fold (one job per section)

1. **Latency specs** — TTFF + steady-state FPS per featured pipeline (not price)
2. **How it works** — graph → Livepeer BYOC → billable seconds (3 steps max)
3. **Bounded portability** — same Comfy graph, curated node/model list link to `/pipelines`
4. **Final CTA**

## Copy constraints

- Lead with economics lightly; do not center Midjourney/batch framing
- Agent/MCP is a **footer mention or secondary link** until beat two — not hero
- No "any workflow" — "workflows built from our supported nodes & models"

## Motif

Connector-curve section dividers; port dots as list bullets. Keep motion to edge-flow + cost tick.
